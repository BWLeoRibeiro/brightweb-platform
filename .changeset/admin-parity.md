---
"@brightweblabs/module-admin": minor
---

Add the MQ-parity packaged Admin UI for users, roles, invitations, loading, and shell toolbar controls, with an injectable client, Portuguese dictionary, scoped tokens, and a full preview route.
