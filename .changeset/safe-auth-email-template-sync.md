---
"@brightweblabs/core-auth": minor
---

Add a server-only Supabase Management API utility that backs up, synchronizes, and verifies all 12 hosted authentication email subjects and HTML templates without changing unrelated Auth configuration.
