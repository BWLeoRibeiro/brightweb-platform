---
"create-bw-app": minor
"@brightweblabs/module-admin": minor
---

Keep generated apps thin by removing demo pages, local feature components, and helper libraries; mount CRM and Admin package surfaces directly, expose the package-owned `AdminUsersPage`, and omit a Projects route until the package ships default UI.
