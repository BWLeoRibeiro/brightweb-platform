---
"@brightweblabs/app-shell": patch
"@brightweblabs/ui": patch
"@brightweblabs/core-auth": patch
---

Keep notification dismissal and pagination responses scoped to their current configuration and list. Recover theme preferences when browser storage cannot be acquired. Split dashboard views and shared request loaders without changing their rendered output, share typography class merging through the UI utility export, and remove unused private authentication and dashboard code.
