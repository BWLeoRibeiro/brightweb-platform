---
"create-bw-app": patch
---

Gate each pending destructive migration after the current cursor, share and validate module-aware generated outputs, and preserve exact-path customization decisions, drift baselines, and removed-module migration history during forced adoption and module remove/re-add transitions.
