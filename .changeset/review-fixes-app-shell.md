---
"@brightweblabs/app-shell": patch
---

Surface server publicError envelopes ({error: {code, message}}) in dashboard section errors instead of always falling back to generic copy, and treat data-less error payloads as envelopes.
