---
"create-bw-app": minor
---

Add opt-in `bw setup social-media` with an app-owned empty content plan, thin page, and composed navigation. Preflight installed support and safe paths, preserve existing client files on reruns and updates, and support a write-free dry run.
