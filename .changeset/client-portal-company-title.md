---
"@brightweblabs/app-shell": patch
"create-bw-app": patch
---

Display the configured company name beside the client portal logo instead of a hardcoded BrightWeb title. Update the starter compatibility defaults to consume the corrected shell package.
