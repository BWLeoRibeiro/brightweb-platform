---
"create-bw-app": patch
---

Preserve app-owned MDX package dependencies during removal, reject unknown migration cursors consistently in diagnostics, and reuse the shared filesystem boundary checks during feature setup.
