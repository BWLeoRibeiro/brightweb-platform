---
"create-bw-app": patch
---

Keep database ordering implementations in sync, load core compatibility from the shared catalog, include the shell override in agent context, and load CRM tokens in the playground route.
