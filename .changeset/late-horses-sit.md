---
"create-bw-app": minor
---

Refresh the Brightweb database scaffold to use a clean v1 module baseline instead of carrying legacy transitional migration history. Remove BeGreen-specific database references from the generated platform surfaces and align the repo docs with Brightweb-owned forward migration history.
