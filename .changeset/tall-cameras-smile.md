---
"create-bw-app": patch
---

Ship the selected platform Supabase migration baseline in published `create-bw-app` scaffolds, including the module registry, generated client stack metadata, the modular shared SQL sources, and a Supabase CLI-ready `supabase/migrations` output.
