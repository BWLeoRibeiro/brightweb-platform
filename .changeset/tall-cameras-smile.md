---
"create-bw-app": patch
---

Ship the selected platform Supabase migration baseline in published `create-bw-app` scaffolds, including the module registry, resolved shared SQL migrations, and generated client stack metadata.
