---
"create-bw-app": patch
---

Align published Brightweb dependency defaults with the current package versions so newly scaffolded platform apps do not install stale package releases that are missing supported subpath exports such as `@brightweblabs/core-auth/shared`.
