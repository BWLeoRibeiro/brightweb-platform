---
"create-bw-app": minor
---

Stop generating `.env.local` in platform app scaffolds and keep `.env.example` as the single generated environment template. Update the generated starter copy and docs to instruct developers to copy `.env.example` to `.env.local` when they need local credentials or overrides.
