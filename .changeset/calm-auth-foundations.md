---
"@brightweblabs/core-auth": patch
---

Make authentication surfaces self-contained so card padding, hierarchy, labels, support copy, and responsive form geometry no longer depend on consumer Tailwind source scanning. The package now owns accessible typography, spacing, and muted-color roles for every auth route.
