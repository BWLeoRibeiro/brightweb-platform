---
"create-bw-app": patch
---

Publish the shared project access migrations and generated shell permission wiring so existing BrightWeb applications receive the complete `module-projects` 0.18.5 database and UI contract through `bw upgrade`.
