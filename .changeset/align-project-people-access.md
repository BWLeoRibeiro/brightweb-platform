---
"@brightweblabs/module-projects": patch
---

Align project people and access management across the UI, API, and database boundary: limit project creation and permanent deletion to global administrators; allow projects to start without a manager; give project managers operational control; let collaborators update only the state of their own assigned tasks; keep observers read-only; restrict task assignees to active managers and collaborators; block unsafe team removal while open work is assigned; expose eligible staff identities safely; and simplify client access to the project organization.
