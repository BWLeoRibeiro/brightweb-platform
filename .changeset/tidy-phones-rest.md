---
"@brightweblabs/ui": patch
---

Render phone country calling codes as fixed, non-selectable prefixes so users can only edit the subscriber number, while preserving full E.164 values for controlled and native form submissions.
