---
"create-bw-app": minor
---

Add `bw adopt`, `bw diff`, and `bw remove` for safely bringing legacy apps under the manifest contract, inspecting scaffold drift, and removing module package wiring without touching migration history.
